import Claim from '../commands/claim'

describe('Claim Command', () => {
	it('Should create new User if one doesn\'t exist', () => {
		expect(0).toBe(1)
	})

	it('Should fetch existing user if one exists', () => {
		expect(0).toBe(1)
	})

	it('Should get a random amount of bux for each claim', () => {
		expect(0).toBe(1)
	})

	it('Should update user record with the claimAmount', () => {
		expect(0).toBe(1)
	})

	it('Should not allow a claim when the user is on cooldown', () => {
		expect(0).toBe(1)
	})
})